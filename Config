package.FizzyOpsTool = {

    interfaces = (1.0);

    build-system = happytrails;

    build-tools = {
        1.0 = {
            # If you need JDK8 runtime-compatible jars, or need to build for a
            # deprecated platform like RHEL5_64, replace 'jdk11' with 'jdk8'.
            JavaBuildAndTestMin = jdk11;
            HappierTrails = 3.5;
        };
    };

    dependencies = {
        1.0 = {
        };
    };

    test-dependencies = {
        1.0 = {
            # If only using JUnit 4, replace with 'junit = 4.12;". For mixed
            # JUnit 4 and 5 tests, keep this and add 'JUnit-4-12-migration = 5.x;'
            JUnit5 = 5.x;
        };
    };

    # For more information on how to declare targets for HappyTrails packages
    # see: https://w.amazon.com/?BrazilBuildSystem/HappyTrails/GettingStarted
    targets = {
        FizzyOpsTool-1.0 = { type = javalibrary; };
        };

};
